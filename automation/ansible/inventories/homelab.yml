# Usage: ansible-playbook -i inventories/homelab.yml playbooks/services/deploy-k8s-node.yml

all:
  children:
    # Proxmox hosts
    # proxmox_hosts:
    #   hosts:
    #     pve-server1:
    #       ansible_host: 192.168.1.100  # placeholder
    #       ansible_user: root
          
    # VMs that will become K8s nodes
    k8s_nodes:
      hosts:
        k8s-test-vm:
          ansible_host: 192.168.0.94  # placeholder
          ansible_user: garrett         # placeholder
          node_role: control-worker
          ansible_ssh_private_key_file: /Users/ghart/.ssh/ansible

          
    # VMs that will run Portainer (can overlap with k8s_nodes)
    portainer_hosts:
      hosts:
        
